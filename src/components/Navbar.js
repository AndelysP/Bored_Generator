import React from 'react';
import logo from '../assets/img/logo.png';

const Navbar = () => {
  return (
    <header>
        <nav>
            <img src={logo} alt="" />
        </nav>
    </header>
  )
}

export default Navbar